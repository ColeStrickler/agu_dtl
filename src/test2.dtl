int width  = 1920;
int height = 1080;
int channels = 3;
int data_size = 4;

int batch_size = 8;
for (int n = 0; n < 8; n++)
{
    for (int c = 0; c < channels; c++)
    {   
        for (int x = 0; x < height*width; x++)
        {
            out = c*(data_size*height*width) + x*(data_size*channels) + n*(channels*data_size*width*height);           
        }
    }
}





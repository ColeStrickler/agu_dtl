int W = 5;
int element_size = 4; 
    

int out_h = 1078;
int out_w = 1918;

for (int oh = 0; oh < 1078; oh++) {
    for (int ow = 0; ow < 1918; ow++) {
        for (int kh = 0; kh < 3; kh++) {
            for (int kw = 0; kw < 3; kw++) {
                out = ((oh + kh) * W + (ow + kw)) * element_size;
            }
        }
    }
}

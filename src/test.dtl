
int element_size = 4;
int row_size = 256;


for (int col = 0; col < 64; col++)
{
	for (int z = 0; z < 3; z++)
	{
		for(int row = 0; row < 5 ; row++)
		{
			out = row_size*element_size*col*row + z;
		}
	}
	
}

int batch_size = 8;
int height = 64;
int width = 64;
int channels = 3;


for (int c = 0; c < channels; c++)
{
    for (int h = 0; h < height; h++)
    {
        for (int n = 0; n < batch_size; n++)
        {
            for (int w = 0; w < width; w++)
            {
                out = w + n*(height*width*channels) + h*width + c*(height*width); 
            }
        }
    }
}
